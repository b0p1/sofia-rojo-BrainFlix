const Hero = () => {
    return (
      <section className="hero">
        <video className="hero__video">
            <source></source>
        </video>
      </section>
    );
  };
  
  export default Hero;